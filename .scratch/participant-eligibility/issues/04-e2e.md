# 04 — E2E coverage


## Goal

End-to-end coverage for the participant & eligibility slice.

## Details

- Fixtures: `seeded-groups.ts` (seed/delete groups + membership, `SEEDED_GROUP_PREFIX`), `seeded-eligibility.ts` (seed/delete grants); reuse `test-users`, `created-users`, `interactions`, `auth`.
- Specs:
  - `participant-groups.spec.ts`: list/empty states, create with validation error, edit, delete with confirm; blocked for `user` role (forbidden page).
  - `participant-group-members.spec.ts`: add member from combobox, duplicate rejected, remove member, candidates exclude admins/banned/members.
  - `schedule-eligibility.spec.ts`: grant user + group, union visibility in eligible list, revoke both, banned user absent, group delete blocked while granted, schedule delete cascades grants, access-rules hub lists schedules.
- Teardown order: grants → groups/members → schedules (reuse global-teardown, add new tables before users).

## Definition of done

- Full suite green locally against the dev server; teardown leaves the DB and bucket clean.

**Blocked by:** 03 — Schedule eligibility.

**Status:** done
